import React from "react";
type Props = {};

export default function MainBtn({}: Props) {
  return (
    <div onClick={()=> {}} className="px-3 py-1 rounded-lg bg-blue-600">Start Conversation</div>
  );
}
